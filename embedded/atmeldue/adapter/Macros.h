#ifndef __MACROS_H_
#define __MACROS_H_

/*

TODO - Define some ARM specific helper MACROS here

#define F_CPU 16000000UL

#define BIT(x)	(1<<x)
#define SET(reg, bits) reg |= bits
#define CLEAR(reg, bits) reg &= ~bits
#define TOGGLE(reg, bits) reg ^= bits;
*/

#endif
